from wordtools import cleanword
from wordtools import has_dashdash
from wordtools import wordset
from wordtools import longestword
from wordtools import extract_words
from wordtools import wordcount

st2 = 'test343434'
st = 'this is my sentence string this is also my test string'
test = 'eu te amo muito baby husky husky'

print(cleanword(st2))
print(has_dashdash(st))
print(wordset(st))
print(longestword(st))
print(extract_words(st))
print(wordcount(st, 'this'))


